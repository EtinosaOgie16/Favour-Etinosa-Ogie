# Hashnode publication host used for the blog (GraphQL API via gql.hashnode.com).
# Defaults to favourogie.hashnode.dev in code if unset — override here or in
# your Vercel project's Environment Variables to point at a different publication.
HASHNODE_PUBLICATION_HOST=favourogie.hashnode.dev
